{
  "name": "messagehub-app",
  "version": "1.0.0",
  "description": "MessageHub - Real-time Chat & WebRTC Calling App",
  "private": true,
  "scripts": {
    "start": "node Backend/index.js",
    "build": "npm --prefix Frontend run build",
    "install:all": "npm install --prefix Backend && npm install --prefix Frontend"
  }
}
